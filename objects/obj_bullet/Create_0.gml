/// @description Insert description here
// You can write your code in this editor
image_angle = direction; // face the right direction

show_debug_message(direction)

depth = 1;

// Bullet speed/direction already set earlier, just add:
hit_list = ds_list_create(); // stores enemies already hit
max_pierce = 2; // number of zombies bullet can pierce before being destroyed
